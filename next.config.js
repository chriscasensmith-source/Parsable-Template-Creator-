/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable Next.js body parser for API routes that handle multipart/form-data
  // (handled per-route via route segment config)
};

module.exports = nextConfig;
